[toolchain]
# Pin the exact compiler so macOS, Windows and CI agree on clippy lints and
# codegen. Bump this deliberately (and update .github/workflows/ci.yml) rather
# than drifting with `stable`.
channel = "1.98.0"
components = ["rustfmt", "clippy"]
targets = []
profile = "default"
